.claude
/target/

# dlib pre-trained model files — downloaded automatically by build.rs.
# Never commit these; they are large binary files (~31 MB combined).
faceauth-core/models/*.dat

# Generated icon raster files — produced by assets/icons/generate.sh at build time.
assets/icons/png/
assets/icons/*.ico
